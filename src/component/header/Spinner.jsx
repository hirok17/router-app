 

const Spinner = () => {
    return (
        <div className="absolute top-[50%] left-[50%]">
              <span className="loading loading-spinner loading-lg text-center text-primary"></span>
        </div>
    );
};

export default Spinner;